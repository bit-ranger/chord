{
  "name": "job",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "release": "webpack build --progress --mode=production",
    "build": "NODE_ENV=development webpack build --progress --watch --mode=development --watch-options-stdin"
  },
  "devDependencies": {
    "webpack": "^5.65.0",
    "webpack-cli": "^4.9.1",
    "copy-webpack-plugin": "^9.0.1"
  }
}
