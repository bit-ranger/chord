version: "0.0.1"

def:
  iter: [ a, b, { "c": "d" } ]


stage:
  sg1:
    step:
      
      sp1:
        action: echo
        args: |
          content:
            {{str (jsonpath "$[?(@.c=='d')]" def.iter)}}
        assert: |
          (eq curr.value.0.c "d")
      
      sp4:
        action: log
        args:
          content:
            $ref: step.sp1.value
            
            
            
            
                    
        
