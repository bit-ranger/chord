version: "0.0.1"

stage:
  bm1:
    step:
      s1:
        exec:
          action: program
          args:
            program: java
            args:
              - -version
            value_as_json: false
            
      s2:
        exec:
          action: program
          args:
            scope: case
            program: python
            args:
              - --version
            value_as_json: false

      s3:
        exec:
          action: program
          args:
            scope: task
            program: python
            args:
              - --version
            value_as_json: false

