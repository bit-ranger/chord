version: "0.0.1"


stage:
  stage1:
    
    step:
      step1:
        let:
          lon: "{{case.origin_lon}}"
          lat: "{{case.origin_lat}}"
          lon_lat: "{{lon}},{{lat}}"
        exec:
          action: log
          args:
            log: |-
              dml >>> update some_table set column1 = '{{lon_lat}}' where id = 1'
    
              

