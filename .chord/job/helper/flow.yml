version: "0.0.1"



stage:
  g1:
    step:
      s1:
        exec:
          action: nop
          args: {}
        
        assert: |
          (all
              (eq (num_add 1 1) 2)
              (eq (num_add 1.1 1) 2.1)
              (gt (num_add 1.5 2.5) 4)
          )
      
     

