version: "0.0.1"

stage:
  stage1:
    
    step:
      step1:
        action: shell
        args:
          code: |-
            ls /
            echo "hello world"
            
        
        assert: |
          (all
              (eq curr.value.1.bar (num case.bar))
          )

