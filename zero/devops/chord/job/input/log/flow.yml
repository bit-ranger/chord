version: "0.0.1"


stage:
    stage1:
        concurrency: 10
        step:
            step1:
                action: log
                args: |-
                    dml >>> update some_table set column1 = '{{data.orgin_lon}}' where column2 = '{{data.origin_lat}}'
    
              

