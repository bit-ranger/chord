version: "0.0.1"


stage:
  stage1:
    
    step:
      step1:
        let:
          lon: "{{case.orgin_lon}}"
          lat: "{{case.origin_lat}}"
        exec:
          action: log
          args:
            content: |-
              dml >>> update some_table set column1 = '{{lon}}' where column2 = '{{lat}}'
    
              

