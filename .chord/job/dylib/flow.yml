version: "0.0.1"


stage:
  stage1:
    
    step:
      step1:
        exec:
          action: dylib
          args:
            dir: /home/runner/work/chord/chord/target/release
        assert: |
          (all
            (eq value.run "1")
          )
    

