task:
  point:
    chain: ["api1"]
  def:
    domain: "www.baidu.com"


point:
    api1:
        type: http
        config:
            url: https://{{domain}}?name={{科目名称}}
        assert: |+
            (eq 科目编号 科目编号)

